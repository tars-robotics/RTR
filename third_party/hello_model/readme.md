# hello_model

A minimal example model that pairs with `rtr_async_sys` to demonstrate the
launch-system / launch-user split.

## Launch the system side
```
cd rtr_async_sys
bash scripts/hello_model/launch_system.sh
```

## Launch the user side
```
cd third_party/hello_model
bash scripts/launch_hello_user.sh
```
