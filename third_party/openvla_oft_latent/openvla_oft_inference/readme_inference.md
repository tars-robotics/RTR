Inference-side helpers (latent variant) consumed by `rtr_async_sys` — model
wrappers, launch scripts, configs.

The symlinking step that mounts these files into `openvla-oft/` is documented
in `docs/environment/openvla_oft.md`.
