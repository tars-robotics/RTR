Users mediate communication between models and the system. Two model integration patterns are planned:
1. Add a model-specific folder under models and expose a model creation interface
2. Create the model in an external repository and communicate with the system through the User interface